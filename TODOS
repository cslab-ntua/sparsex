1. C API
   -- BLAS for matrix operations
   -- PETSc (?)
   -- Matrix tuning
      * [DONE] Pattern setup
   -- [DONE] Matrix I/O
      * [DONE] from file (enriched MM, etc.)
      * [DONE] from CSR
      * [DONE] from CSX file
      * [DONE] to CSX file
   -- [DONE] Matrix access
      * [DONE] set matrix values in CSX
   -- [DONE] Matrix reordering (RCM)
   -- [50%] Multithreaded setup
      * [DONE] Threads/Thread pool?
      * NUMA setup (allocators)
   -- [DONE] Support for symmetrics
   -- [DONE] Timing/Profiling framework
      * SpMV, preproc, etc.
   -- [DONE] Preprocessing optimization
      * CSX file vs. online preproc.
   -- Verbosity control
      * verbosity levels (debug, info, warning, error)
      * logger (standard / streams, low overhead, use of templates)
   -- Baseline benchmarking code for all libs (MKL, OSKI, CSX) out-of-the-box

2. Refactoring
   -- Homogenize variable types (size_t, signed/unsigned)->nrows...
   -- Variable naming
   -- [50%] Code cleanup (also thgoud)
   -- [75%] Classes/Structures revisiting
   -- Memory management (leaks, double free corruptions, etc.)
   -- [DONE] SPM indexing (C++ templates?)

3. User config & installation
   -- autotools (configure;make;install)
   -- config.h

4. Testing
   -- Command-line interface
      * use of options
   -- Test suite

5. Documentation
   -- doxygen
   -- User guide & Manual

6. Demo
   -- Libcsx vs. pOSKI vs. Intel MKL vs. Banded matrices

7. Symmetrics revisiting
8. What about libspm C library?
   -- Rewrite Bench/CheckLoop() in C++ and ditch the rest
